import { it } from "vitest";

it.todo("returns persistent directory entries for the root path");

it.todo("returns all shows for the /shows path");

it.todo("returns all movies for the /movies path");

it.todo("returns episodes for a show season path");

it.todo("returns media entries for a known media item path");
