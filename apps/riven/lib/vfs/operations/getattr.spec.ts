import { it } from "vitest";

it.todo("returns a container directory for the root path");

it.todo("returns a container directory for /movies");

it.todo("returns a container directory for /shows");

it.todo("skips hidden and trash paths");

it.todo("returns ENOENT for unknown paths");

it.todo("returns file stats for known files");
