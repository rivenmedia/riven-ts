import { it } from "vitest";

it.todo(
  "requests downloader plugins sequentially until one returns a valid torrent container",
);

it.todo(
  "throws an UnrecoverableError if no plugins return a valid torrent container",
);
