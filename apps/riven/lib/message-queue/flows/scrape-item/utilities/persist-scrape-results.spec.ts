import { it } from "vitest";

it.todo(
  "returns the updated item if persisting the scrape results is successful",
);

it.todo(
  'throws a MediaItemScrapeErrorIncorrectState error if the item is not in the "indexed" state',
);

it.todo(
  "throws a MediaItemScrapeError error if a validation error occurs whilst persisting the scrape results",
);
