# GraphQL endpoint exposed by apps/riven (default: localhost:3000)
PUBLIC_RIVEN_GRAPHQL_URL=http://localhost:3000
# Set when introspecting the schema during codegen against a remote dev instance
RIVEN_SCHEMA_URL=http://localhost:3000
