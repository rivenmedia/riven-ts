# The GraphQL endpoint of the running Riven instance to connect to.
RIVEN_TUI_GRAPHQL_URL="http://localhost:3000/"

# Optional API key, sent as an `Authorization: Bearer` header.
RIVEN_TUI_API_KEY=""
