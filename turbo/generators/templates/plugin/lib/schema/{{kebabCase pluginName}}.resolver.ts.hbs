import { PluginDataSource } from "@rivenmedia/plugin-sdk";

import { Query, Resolver } from "type-graphql";

import { {{pascalCase pluginName}}API } from "../datasource/{{kebabCase pluginName}}.datasource.ts";
import { pluginConfig } from "../{{kebabCase pluginName}}-plugin.config.ts";

@Resolver()
export class {{pascalCase pluginName}}Resolver {
  @Query(() => Boolean)
  {{camelCase pluginName}}IsValid(
    @PluginDataSource(pluginConfig.name, {{pascalCase pluginName}}API) api: {{pascalCase pluginName}}API,
  ): Promise<boolean> {
    return api.validate();
  }
}
