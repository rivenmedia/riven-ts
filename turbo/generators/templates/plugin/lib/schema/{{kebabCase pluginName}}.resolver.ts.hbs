import type { {{pascalCase pluginName}}ContextSlice } from "../datasource/{{kebabCase pluginName}}.datasource.ts";
import { pluginConfig } from "../{{kebabCase pluginName}}-plugin.config.ts";
import { Query, Resolver } from "type-graphql";
import { PluginContext } from "@repo/util-plugin-sdk";

@Resolver()
export class {{pascalCase pluginName}}Resolver {
  @Query((_returns) => Boolean)
  async {{camelCase pluginName}}IsValid(
    @PluginContext(pluginConfig.name) { api }: {{pascalCase pluginName}}ContextSlice,
  ): Promise<boolean> {
    return await api.validate();
  }
}
