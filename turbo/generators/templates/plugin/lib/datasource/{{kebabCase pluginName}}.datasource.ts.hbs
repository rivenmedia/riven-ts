import { BaseDataSource } from "@repo/util-plugin-sdk";

import type { {{pascalCase pluginName}}Settings } from "../{{kebabCase pluginName}}-settings.schema.ts";

class {{pascalCase pluginName}}APIError extends Error {
  public override name = "{{pascalCase pluginName}}APIError";
}

export class {{pascalCase pluginName}}API extends BaseDataSource<{{pascalCase pluginName}}Settings> {
  public override baseURL = "https://{{kebabCase pluginName}}.com/api/";
  public override serviceName = "{{pascalCase pluginName}}";

  public override async validate() {
    try {
      // Implement your own validation logic here
      await this.get('validate');

      return true;
    } catch {
      return false;
    }
  }

  public async fetchData() {
    try {
      const response = await this.get<unknown>("data");

      console.log(response); // oxlint-disable-line no-console
    } catch {
      throw new {{pascalCase pluginName}}APIError("Failed to fetch data");
    }
  }
}
