import { BaseDataSource } from "@repo/core-util-datasource";

export class {{pascalCase pluginName}}APIError extends Error {}

export class {{pascalCase pluginName}}API extends BaseDataSource {
  override baseURL = "https://{{kebabCase pluginName}}.com/api/";
  override serviceName = "{{pascalCase pluginName}}";

  async validate() {
    try {
      // Implement your own validation logic here
      await this.get('validate');

      return true;
    } catch {
      return false;
    }
  }
}

export interface {{pascalCase pluginName}}ContextSlice {
  api: {{pascalCase pluginName}}API;
}
